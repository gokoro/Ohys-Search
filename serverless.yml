service: ohys-search

provider:
  name: aws
  runtime: nodejs14.x
  stage: dev
  region: ap-northeast-2

functions:
  app:
    handler: src/serverless.handler
    events:
      - httpApi: '*'
      - httpApi:
          path: /{any+}
          method: '*'

plugins:
  - serverless-esbuild

custom:
  esbuild:
    packager: yarn
    bundle: true
    minify: true
    plugins: scripts/esbuild-plugins.cjs
    platform: neutral
    target: node14.14

useDotenv: true
